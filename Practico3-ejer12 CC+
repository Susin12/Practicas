#include <iostream>
using namespace std;

int main() {

    int dia, mes, anio;

    cout << "Ingrese dia: ";
    cin >> dia;

    cout << "Ingrese mes: ";
    cin >> mes;

    cout << "Ingrese anio: ";
    cin >> anio;

    if ((mes == 12 && dia >= 21) ||
        mes == 1 || mes == 2 ||
        (mes == 3 && dia < 21)) {

        cout << "Verano" << endl;
    }
    else if ((mes == 3 && dia >= 21) ||
             mes == 4 || mes == 5 ||
             (mes == 6 && dia < 21)) {

        cout << "Otono" << endl;
    }
    else if ((mes == 6 && dia >= 21) ||
             mes == 7 || mes == 8 ||
             (mes == 9 && dia < 21)) {

        cout << "Invierno" << endl;
    }
    else {

        cout << "Primavera" << endl;
    }

    return 0;
}
